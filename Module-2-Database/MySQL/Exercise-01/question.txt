Exercise 01: User Upcoming Events

Show a list of all upcoming events a user is registered for in their city, sorted by date.

Parameters: :user_id (replace with the user's id)

Requirements: Use `Users`, `Registrations`, and `Events` tables. Only include events whose `start_date` is today or later and where the event `city` matches the user's `city`.

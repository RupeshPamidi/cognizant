Exercise 22: Duplicate Registrations Check

Detect if a user has been registered more than once for the same event.

Requirements: Use `Registrations`. Return `user_id`, `event_id`, `duplicate_count`.

